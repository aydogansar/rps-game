import type { NextPage } from "next";
import { Container, Header, PlayGround } from "components";

const Home: NextPage = () => {
  return (
    <Container>
      <Header />
      <PlayGround />
    </Container>
  );
};

export default Home;
